<UserControl x:Class="Instrux.Application.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="960">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="560" />
            <ColumnDefinition Width="380" />
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource Card}" Margin="0,0,18,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel Margin="0,0,0,18">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Content="‹" Style="{StaticResource SoftButton}" Command="{Binding PreviousMonthCommand}" Margin="0,0,8,0" />
                        <Button Content="›" Style="{StaticResource SoftButton}" Command="{Binding NextMonthCommand}" />
                    </StackPanel>
                    <TextBlock Text="{Binding MonthTitle}" Style="{StaticResource SectionTitle}" />
                </DockPanel>

                <UniformGrid Grid.Row="1" Columns="7" Margin="0,0,0,8">
                    <TextBlock Text="Sun" Style="{StaticResource CaptionText}" />
                    <TextBlock Text="Mon" Style="{StaticResource CaptionText}" />
                    <TextBlock Text="Tue" Style="{StaticResource CaptionText}" />
                    <TextBlock Text="Wed" Style="{StaticResource CaptionText}" />
                    <TextBlock Text="Thu" Style="{StaticResource CaptionText}" />
                    <TextBlock Text="Fri" Style="{StaticResource CaptionText}" />
                    <TextBlock Text="Sat" Style="{StaticResource CaptionText}" />
                </UniformGrid>

                <ItemsControl Grid.Row="2" ItemsSource="{Binding Days}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="7" Rows="6" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{x:Null}" 
                                    Command="{Binding DataContext.SelectDayCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" 
                                    BorderThickness="0" 
                                    Padding="0" 
                                    Background="Transparent" 
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Button.Template>
                                
                                <Border x:Name="DayCell" CornerRadius="18" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="10" Margin="3" MinHeight="96">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#F4F8FE" />
                                            <Setter Property="Opacity" Value="1" />
                                            
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Events.Count}" Value="0">
                                                    <Setter Property="Background" Value="White" />
                                                </DataTrigger>

                                                <DataTrigger Binding="{Binding IsCurrentMonth}" Value="False">
                                                    <Setter Property="Opacity" Value="0.38" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource PrimarySoft}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="Background" Value="#DDF5EF" />
                                                    <Setter Property="BorderBrush" Value="{StaticResource SecondaryLight}" />
                                                    <Setter Property="BorderThickness" Value="2" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Date.Day}" FontWeight="SemiBold" />
                                        <ItemsControl ItemsSource="{Binding Events}" Margin="0,8,0,0">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="{Binding Category, Converter={StaticResource EventCategoryToBrushConverter}}" CornerRadius="8" Padding="6,3" Margin="0,0,0,4">
                                                        <TextBlock Text="{Binding Title}" Foreground="White" FontSize="11" TextTrimming="CharacterEllipsis" />
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                            <svgc:SvgViewbox Source="/Resources/calendar-fold.svg" Width="24" Height="24" Margin="0,0,10,0" />
                            <TextBlock Text="Create Event" Style="{StaticResource SectionTitle}" />
                        </StackPanel>

                        <TextBlock Text="{Binding SelectedDateTitle}" Style="{StaticResource CaptionText}" Margin="0,0,0,10" />
                        <TextBlock Text="Event name" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                        <TextBox Style="{StaticResource InputBox}" Text="{Binding NewEventTitle, UpdateSourceTrigger=PropertyChanged}" MaxLength="200" ToolTip="e.g., Quiz deadline" Margin="0,0,0,0" />
                        <TextBlock Text="e.g., Quiz deadline" Foreground="{StaticResource MutedInkBrush}" FontSize="11" Margin="4,0,0,8" />
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Category" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                                <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Padding="12,8" ToolTip="Categorize this event" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="Start time" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                                <TextBox Style="{StaticResource InputBox}" Text="{Binding StartTimeText, UpdateSourceTrigger=PropertyChanged}" MaxLength="10" ToolTip="HH:mm or h:mm AM/PM format" />
                                <TextBlock Text="e.g., 9:00 AM" Foreground="{StaticResource MutedInkBrush}" FontSize="11" Margin="4,0,0,0" />
                            </StackPanel>
                        </Grid>
                        <TextBlock Text="Notes" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                        <TextBox Style="{StaticResource InputBox}" Text="{Binding EventNotes, UpdateSourceTrigger=PropertyChanged}" MaxLength="500" ToolTip="Optional notes" Margin="0,0,0,0" />
                        <TextBlock Text="e.g., Bring calculators next session" Foreground="{StaticResource MutedInkBrush}" FontSize="11" Margin="4,0,0,12" />
                        <Button Content="Add event" Style="{StaticResource PrimaryButton}" Command="{Binding AddEventCommand}" HorizontalAlignment="Left" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Selected Day" Style="{StaticResource SectionTitle}" />
                        <TextBlock Text="{Binding SelectedDateTitle}" Style="{StaticResource CaptionText}" Margin="0,2,0,12" />
                        <ItemsControl ItemsSource="{Binding TodayAgenda}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource PrimarySoft}" CornerRadius="18" Padding="14" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />
                                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                    <Border Background="{Binding Category, Converter={StaticResource EventCategoryToBrushConverter}}" CornerRadius="8" Padding="8,3">
                                                        <TextBlock Text="{Binding Category}" Foreground="White" FontSize="12" FontWeight="SemiBold" />
                                                    </Border>
                                                    <TextBlock Text="{Binding StartTime, StringFormat={}{0:h:mm tt}}" Visibility="{Binding StartTime, Converter={StaticResource NullToVisibilityConverter}}" Foreground="{StaticResource MutedInkBrush}" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0" />
                                                </StackPanel>
                                                <TextBlock Text="{Binding Notes}" Visibility="{Binding Notes, Converter={StaticResource NullToVisibilityConverter}}" Style="{StaticResource CaptionText}" Foreground="{StaticResource InkBrush}" Margin="0,6,0,0" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Button Grid.Column="1" Content="Delete" Style="{StaticResource SoftButton}" Command="{Binding DataContext.DeleteEventCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" VerticalAlignment="Top" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="Upcoming" Style="{StaticResource SectionTitle}" />
                        <ItemsControl ItemsSource="{Binding UpcomingEvents}" Margin="0,12,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,12" Margin="0,0,0,12">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Date, StringFormat={}{0:dddd, MMM d}}" Style="{StaticResource CaptionText}" />
                                                <TextBlock Text="{Binding StartTime, StringFormat={}{0:  ·  h:mm tt}}" Visibility="{Binding StartTime, Converter={StaticResource NullToVisibilityConverter}}" Style="{StaticResource CaptionText}" />
                                            </StackPanel>
                                            <TextBlock Text="{Binding Category}" Style="{StaticResource CaptionText}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
