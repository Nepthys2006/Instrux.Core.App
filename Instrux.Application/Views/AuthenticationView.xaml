<UserControl x:Class="Instrux.Application.Views.AuthenticationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d"
             d:DesignHeight="620" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.15*" />
            <ColumnDefinition Width="0.85*" />
        </Grid.ColumnDefinitions>

        <Border CornerRadius="28,0,0,28" Padding="44">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#2C5EAD" Offset="0" />
                    <GradientStop Color="#1591DC" Offset="0.55" />
                    <GradientStop Color="#005461" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <StackPanel VerticalAlignment="Center">
                    <svgc:SvgViewbox Source="/Resources/MainLogo/Instrux_Logo.svg" Width="360" Height="300" HorizontalAlignment="Center" Margin="0,0,0,10" />
                    <TextBlock Text="Instrux" Foreground="White" FontSize="48" FontWeight="SemiBold" />
                    <TextBlock Text="Your classroom workflow, organized into one focused desktop workspace." Foreground="#EAF6FF" FontSize="18" TextWrapping="Wrap" MaxWidth="380" Margin="0,12,0,28" />
                    <StackPanel Orientation="Horizontal">
                        <svgc:SvgViewbox Source="pack://application:,,,/Resources/square-check-big.svg" Width="22" Height="22" Margin="0,0,10,0" />
                        <TextBlock Text="Attendance, grades, todos, calendar, and materials." Foreground="White" FontSize="14" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="0,28,28,0" Padding="42">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}" FontSize="32" FontWeight="SemiBold" Foreground="{StaticResource InkBrush}" />
                <TextBlock Text="{Binding Subtitle}" Style="{StaticResource CaptionText}" TextWrapping="Wrap" Margin="0,6,0,28" />

                <StackPanel Visibility="{Binding IsSignUp, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="Full name" Style="{StaticResource CaptionText}" />
                    <TextBox Style="{StaticResource InputBox}" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,12" />
                    <TextBlock Text="Nickname" Style="{StaticResource CaptionText}" />
                    <TextBox Style="{StaticResource InputBox}" Text="{Binding Nickname, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,12" />
                </StackPanel>

                <TextBlock Text="Email" Style="{StaticResource CaptionText}" />
                <TextBox Style="{StaticResource InputBox}" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,12" />

                <TextBlock Text="Password" Style="{StaticResource CaptionText}" />
                <PasswordBox x:Name="PasswordInput" PasswordChanged="OnPasswordChanged" Style="{StaticResource PasswordInputBox}" Margin="0,6,0,18" />

                <Border Background="{StaticResource DangerSoft}" CornerRadius="12" Padding="12" Margin="0,0,0,14" Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" />
                </Border>

                <Button Content="{Binding PrimaryActionText}" Style="{StaticResource PrimaryButton}" Command="{Binding SubmitCommand}" HorizontalAlignment="Stretch" />
                <Button Content="{Binding ToggleActionText}" Style="{StaticResource SoftButton}" Command="{Binding ToggleModeCommand}" HorizontalAlignment="Stretch" Margin="0,12,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
