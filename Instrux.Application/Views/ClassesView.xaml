<UserControl x:Class="Instrux.Application.Views.ClassesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="960">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource Card}" Margin="0,0,18,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Classes" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="Create class spaces with rosters, grades, attendance and files." Style="{StaticResource CaptionText}" TextWrapping="Wrap" Margin="0,4,0,16" />

                    <TextBlock Text="Name of class" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                    <TextBox Style="{StaticResource InputBox}" Text="{Binding NewClassName, UpdateSourceTrigger=PropertyChanged}" MinWidth="220" Margin="0,0,0,8" ToolTip="Example: Grade 8 Mathematics" />
                    <TextBlock Text="Section" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                    <TextBox Style="{StaticResource InputBox}" Text="{Binding NewClassSection, UpdateSourceTrigger=PropertyChanged}" MinWidth="220" Margin="0,0,0,8" ToolTip="Example: Section A" />
                    <TextBlock Text="Subject" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}" Padding="12,8" Margin="0,0,0,10" />
                    <Button Style="{StaticResource PrimaryButton}" Command="{Binding CreateClassCommand}" HorizontalContentAlignment="Center" Margin="0,0,0,18">
                        <StackPanel Orientation="Horizontal">
                            <svgc:SvgViewbox Source="/Resources/plus.svg" Width="16" Height="16" Margin="0,0,8,0" />
                            <TextBlock Text="Create class" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <ListBox ItemsSource="{Binding Classes}" SelectedItem="{Binding SelectedClass}" BorderThickness="0" Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="20" Padding="14" Margin="0,0,0,8" Background="{StaticResource PrimarySoft}" Width="270">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Background="{Binding CoverColor, Converter={StaticResource StringToBrushConverter}}" CornerRadius="6" />
                                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="15" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Subject}" Style="{StaticResource CaptionText}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource Card}" Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding SelectedClass.Name}" FontSize="30" VerticalAlignment="Center" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding SelectedClass.Subject}" Style="{StaticResource CaptionText}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Border CornerRadius="18" Padding="16,10" Background="{Binding SelectedClass.CoverColor, Converter={StaticResource StringToBrushConverter}}" Margin="0,0,10,0">
                            <TextBlock Text="{Binding SelectedClass.Section}" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </Border>
                        <Button Content="Delete class" Style="{StaticResource PrimaryButton}" Background="{StaticResource DangerBrush}" Command="{Binding DeleteClassCommand}" />
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Background="{StaticResource PrimarySoft}" CornerRadius="18" Padding="14,10" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <svgc:SvgViewbox Source="/Resources/search.svg" Width="18" Height="18" Margin="0,0,10,0" />
                        <TextBox Grid.Column="1" Style="{StaticResource InputBox}" Text="{Binding ClassSearch, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Background="White" ToolTip="Search students by name, ID, or email" />
                    </Grid>
                </Border>

                <UniformGrid Grid.Row="2" Columns="3" Margin="0,0,0,16">
                    <Border CornerRadius="20" Padding="16" Background="{StaticResource PrimarySoft}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="Written Works" Style="{StaticResource CaptionText}" />
                            <TextBlock Text="{Binding WrittenWorksPercent}" FontSize="26" FontWeight="SemiBold" Foreground="{StaticResource PrimaryDark}" />
                        </StackPanel>
                    </Border>
                    <Border CornerRadius="20" Padding="16" Background="#DDF5EF" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="Performance Tasks" Style="{StaticResource CaptionText}" />
                            <TextBlock Text="{Binding PerformanceTasksPercent}" FontSize="26" FontWeight="SemiBold" Foreground="{StaticResource SecondaryMid}" />
                        </StackPanel>
                    </Border>
                    <Border CornerRadius="20" Padding="16" Background="{StaticResource DangerSoft}">
                        <StackPanel>
                            <TextBlock Text="Quarterly Assessment" Style="{StaticResource CaptionText}" />
                            <TextBlock Text="{Binding QuarterlyAssessmentPercent}" FontSize="26" FontWeight="SemiBold" Foreground="{StaticResource DangerBrush}" />
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <TabControl Grid.Row="3" Background="Transparent" BorderThickness="0">
                    <TabItem Header="Roster">
                        <Grid Margin="0,18,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Text="Find designated student" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                                <TextBox Style="{StaticResource InputBox}" Text="{Binding StudentSearch, UpdateSourceTrigger=PropertyChanged}" MinWidth="280" ToolTip="Search roster by student name" />
                            </StackPanel>

                            <Grid Grid.Row="1" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Student full name" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                                    <TextBox Style="{StaticResource InputBox}" Text="{Binding NewStudentName, UpdateSourceTrigger=PropertyChanged}" MinWidth="280" ToolTip="Example: Juan Dela Cruz" />
                                </StackPanel>
                                <Button Grid.Column="1" Content="Add" Style="{StaticResource PrimaryButton}" Command="{Binding AddStudentCommand}" Margin="10,0,0,0" />
                            </Grid>

                            <DataGrid Grid.Row="2" ItemsSource="{Binding Students}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" BorderThickness="0" RowHeight="54" HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Student.FullName}" Width="*" />
                                    <DataGridTextColumn Header="Total Attendance" Binding="{Binding TotalAttendance}" Width="105" />
                                    <DataGridTextColumn Header="Present" Binding="{Binding TotalPresent}" Width="80" />
                                    <DataGridTextColumn Header="Late" Binding="{Binding TotalLate}" Width="70" />
                                    <DataGridTextColumn Header="Absences" Binding="{Binding TotalAbsences}" Width="90" />
                                    <DataGridTextColumn Header="Excused" Binding="{Binding TotalExcused}" Width="90" />
                                    <DataGridTemplateColumn Header="Action" Width="125">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Delete" Command="{Binding DataContext.DeleteStudentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SoftButton}" Padding="10,6" MinWidth="92" HorizontalAlignment="Center" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="Attendance">
                        <Grid Margin="0,18,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                <TextBlock Text="Attendance date" VerticalAlignment="Center" Margin="0,0,10,0" />
                                <DatePicker SelectedDate="{Binding AttendanceDate}" />
                            </StackPanel>
                            <DataGrid Grid.Row="1" ItemsSource="{Binding AttendanceRows}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" BorderThickness="0" RowHeight="56" HorizontalScrollBarVisibility="Auto">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Student Name" Binding="{Binding Student.FullName}" Width="220" IsReadOnly="True" />
                                    <DataGridTextColumn Header="Current" Binding="{Binding Status}" Width="110" IsReadOnly="True" />
                                    <DataGridTemplateColumn Header="Present" Width="135">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding PresentMarker}" Command="{Binding DataContext.MarkPresentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SoftButton}" Padding="9,6" MinWidth="108" HorizontalAlignment="Center" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Late" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding LateMarker}" Command="{Binding DataContext.MarkLateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SoftButton}" Padding="9,6" MinWidth="96" HorizontalAlignment="Center" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Absent" Width="135">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding AbsentMarker}" Command="{Binding DataContext.MarkAbsentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SoftButton}" Padding="9,6" MinWidth="108" HorizontalAlignment="Center" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Excused" Width="145">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding ExcusedMarker}" Command="{Binding DataContext.MarkExcusedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SoftButton}" Padding="9,6" MinWidth="116" HorizontalAlignment="Center" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="Grades">
                        <Grid Margin="0,18,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Assessment name" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                                    <TextBox Style="{StaticResource InputBox}" Text="{Binding NewAssessmentName, UpdateSourceTrigger=PropertyChanged}" ToolTip="Example: Quiz 1" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                    <TextBlock Text="Type" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                                    <ComboBox ItemsSource="{Binding AssessmentTypes}" SelectedItem="{Binding NewAssessmentType}" Padding="12,8" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="8,0,0,0">
                                    <TextBlock Text="Over score / Max" Style="{StaticResource CaptionText}" Margin="0,0,0,4" />
                                    <TextBox Style="{StaticResource InputBox}" Text="{Binding NewAssessmentMaxScore, UpdateSourceTrigger=PropertyChanged}" ToolTip="Example: 100" />
                                </StackPanel>
                                <Button Grid.Column="3" Content="Add assessment" Style="{StaticResource PrimaryButton}" Command="{Binding AddAssessmentCommand}" Margin="10,18,0,0" />
                            </Grid>

                            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal" Margin="220,0,0,0">
                                    <ItemsControl ItemsSource="{Binding Assessments}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="112" CornerRadius="14" Background="{StaticResource SurfaceMutedBrush}" Padding="8" Margin="0,0,8,0">
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                        <TextBlock Text="{Binding MaxScore, StringFormat=Max: {0:0.##}}" Style="{StaticResource CaptionText}" />
                                                        <TextBlock Text="{Binding Type}" Style="{StaticResource CaptionText}" />
                                                        <Button Content="Delete" Command="{Binding DataContext.DeleteAssessmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SoftButton}" Padding="4,2" FontSize="11" HorizontalAlignment="Center" Margin="0,4,0,0" />
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </ScrollViewer>

                            <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding GradeRows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1" Padding="0,8">
                                                <StackPanel Orientation="Horizontal">
                                                    <StackPanel Width="220" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Student.FullName}" FontWeight="SemiBold" />
                                                        <TextBlock Text="{Binding Student.StudentId}" Style="{StaticResource CaptionText}" />
                                                    </StackPanel>

                                                    <ItemsControl ItemsSource="{Binding Cells}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <StackPanel Orientation="Horizontal" />
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <TextBox Width="112" Style="{StaticResource InputBox}" Text="{Binding Value, UpdateSourceTrigger=LostFocus, StringFormat={}{0:0.##}}" Margin="0,0,8,0" ToolTip="Enter score" />
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <StackPanel Width="230" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                                                        <TextBlock Text="{Binding WrittenWorksAverage, StringFormat=WW {0:0.#}}" Style="{StaticResource CaptionText}" Margin="0,0,10,0" />
                                                        <TextBlock Text="{Binding PerformanceTasksAverage, StringFormat=PT {0:0.#}}" Style="{StaticResource CaptionText}" Margin="0,0,10,0" />
                                                        <TextBlock Text="{Binding QuarterlyAssessmentAverage, StringFormat=QA {0:0.#}}" Style="{StaticResource CaptionText}" />
                                                    </StackPanel>

                                                    <StackPanel Width="120" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding InitialGrade, StringFormat={}{0:0.##}%}" FontSize="18" FontWeight="SemiBold" />
                                                        <TextBlock Text="{Binding Standing}" Style="{StaticResource CaptionText}" />
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </TabItem>

                    <TabItem Header="Content">
                        <Grid Margin="0,18,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Button Content="Upload file" Style="{StaticResource PrimaryButton}" Command="{Binding UploadContentCommand}" HorizontalAlignment="Left" Margin="0,0,0,14" />

                            <ScrollViewer Grid.Row="1">
                                <ItemsControl ItemsSource="{Binding ContentItems}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="280" Height="178" CornerRadius="22" Padding="18" Background="{StaticResource SurfaceMutedBrush}" Margin="0,0,14,14">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>
                                                    <StackPanel>
                                                        <svgc:SvgViewbox Source="/Resources/file-text.svg" Width="24" Height="24" HorizontalAlignment="Left" />
                                                        <TextBlock Text="{Binding Title}" FontSize="17" FontWeight="SemiBold" Margin="0,14,0,4" TextTrimming="CharacterEllipsis" />
                                                        <TextBlock Text="{Binding Type}" Style="{StaticResource CaptionText}" />
                                                        <TextBlock Text="{Binding Description}" Style="{StaticResource CaptionText}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" />
                                                    </StackPanel>
                                                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                                                        <Button Content="Open" Command="{Binding DataContext.OpenContentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" />
                                                        <Button Content="Delete" Command="{Binding DataContext.DeleteContentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource SoftButton}" />
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Border>
    </Grid>
</UserControl>
