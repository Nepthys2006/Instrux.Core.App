<UserControl x:Class="Instrux.Application.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="960">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            
            <Border CornerRadius="32" Padding="20" Margin="0,0,0,18">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#2C5EAD" Offset="0" />
                        <GradientStop Color="#1591DC" Offset="0.58" />
                        <GradientStop Color="#3BC1A8" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="180" />
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <svgc:SvgViewbox Grid.Row="0" Source="/Resources/MainLogo/waving_hand_icon.svg" Width="64" Height="64" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,10"/>
                        <TextBlock Grid.Row="0" Text="{Binding Greeting}" Foreground="White" FontSize="40" FontWeight="SemiBold" VerticalAlignment="Bottom" />
                        <TextBlock Grid.Row="1" Text="{Binding Today}" Foreground="#E5E9FF" FontSize="20" VerticalAlignment="Bottom" />
                    </Grid>
                    
                    <svgc:SvgViewbox Grid.Column="1" Source="/Resources/MainLogo/Instrux_Logo.svg" Width="160" Height="160" HorizontalAlignment="Left" Opacity="0.95" />
                </Grid>
            </Border>

            <WrapPanel>
                <Border Style="{StaticResource Card}" Width="210">
                    <StackPanel>
                        <svgc:SvgViewbox Source="/Resources/presentation.svg" Width="24" Height="24" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding ClassCount}" FontSize="34" FontWeight="SemiBold" Margin="0,14,0,0" />
                        <TextBlock Text="Active classes" Style="{StaticResource CaptionText}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Width="210">
                    <StackPanel>
                        <svgc:SvgViewbox Source="/Resources/user.svg" Width="24" Height="24" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding StudentCount}" FontSize="34" FontWeight="SemiBold" Margin="0,14,0,0" />
                        <TextBlock Text="Students tracked" Style="{StaticResource CaptionText}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Width="210">
                    <StackPanel>
                        <svgc:SvgViewbox Source="/Resources/square-check-big.svg" Width="24" Height="24" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding TasksDueToday}" FontSize="34" FontWeight="SemiBold" Margin="0,14,0,0" />
                        <TextBlock Text="Due today" Style="{StaticResource CaptionText}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Width="210">
                    <StackPanel>
                        <svgc:SvgViewbox Source="/Resources/calendar-fold.svg" Width="24" Height="24" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding AttendanceMarkedToday}" FontSize="34" FontWeight="SemiBold" Margin="0,14,0,0" />
                        <TextBlock Text="Attendance marks" Style="{StaticResource CaptionText}" />
                    </StackPanel>
                </Border>
            </WrapPanel>

            <StackPanel>
                
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Class Workspaces" Style="{StaticResource SectionTitle}" />
                        <TextBlock Text="Quick glance at the classes you manage." Style="{StaticResource CaptionText}" Margin="0,2,0,16" />
                        
                        <ItemsControl ItemsSource="{Binding Classes}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{x:Null}" 
                                            Command="{Binding DataContext.OpenClassCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" 
                                            BorderThickness="0" 
                                            Padding="0" 
                                            Background="Transparent" 
                                            Cursor="Hand" 
                                            Margin="0,0,14,14">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter />
                                            </ControlTemplate>
                                        </Button.Template>
                                        
                                        <Border Width="250" Height="150" CornerRadius="24" Padding="18" Background="{Binding CoverColor, Converter={StaticResource StringToBrushConverter}}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                
                                                <StackPanel Grid.Row="0">
                                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="21" FontWeight="SemiBold" TextWrapping="Wrap" MaxHeight="58" TextTrimming="CharacterEllipsis" />
                                                    <TextBlock Text="{Binding Subject}" Foreground="#F2F4FF" FontSize="14" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" />
                                                </StackPanel>
                                                
                                                <TextBlock Grid.Row="1" Text="{Binding Section}" Foreground="#F2F4FF" VerticalAlignment="Bottom" TextTrimming="CharacterEllipsis" />
                                            </Grid>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                    </StackPanel>
                </Border>

                <UniformGrid Columns="2">
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Upcoming" Style="{StaticResource SectionTitle}" />
                            <ItemsControl ItemsSource="{Binding TodayEvents}" Margin="0,10,0,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1" Padding="0,10">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding Date, StringFormat={}{0:MMM d}}" Style="{StaticResource CaptionText}" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Focus Tasks" Style="{StaticResource SectionTitle}" />
                            <ItemsControl ItemsSource="{Binding FocusTodos}" Margin="0,10,0,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel Margin="0,0,0,10">
                                            <Border DockPanel.Dock="Left" Width="8" Height="32" CornerRadius="6" Background="{Binding Priority, Converter={StaticResource PriorityToBrushConverter}}" Margin="0,0,10,0" />
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding Priority}" Style="{StaticResource CaptionText}" />
                                            </StackPanel>
                                        </DockPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </UniformGrid>
            </StackPanel>
            
        </StackPanel>
    </ScrollViewer>
</UserControl>
