<UserControl x:Class="Instrux.Application.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="960">
    <ScrollViewer>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource Card}" Margin="0,0,18,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,22">
                        <svgc:SvgViewbox Source="/Resources/MainLogo/Instrux_Logo.svg" Width="84" Height="84" Margin="0,0,16,0" VerticalAlignment="Center" />
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Teacher Profile" Style="{StaticResource SectionTitle}" />
                            <TextBlock Text="Keep your workspace identity clean and professional." Style="{StaticResource CaptionText}" />
                        </StackPanel>
                    </StackPanel>

                    <TextBlock Text="Full name" Style="{StaticResource CaptionText}" />
                    <TextBox Style="{StaticResource InputBox}" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" MaxLength="100" IsEnabled="{Binding IsEditing}" Margin="0,6,0,14" />
                    <TextBlock Text="Nickname" Style="{StaticResource CaptionText}" />
                    <TextBox Style="{StaticResource InputBox}" Text="{Binding Nickname, UpdateSourceTrigger=PropertyChanged}" MaxLength="50" IsEnabled="{Binding IsEditing}" Margin="0,6,0,14" />
                    <TextBlock Text="Email" Style="{StaticResource CaptionText}" />
                    <TextBox Style="{StaticResource InputBox}" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" MaxLength="254" IsEnabled="{Binding IsEditing}" Margin="0,6,0,18" />

                    <StackPanel Orientation="Horizontal">
                        <Button Content="{Binding LockButtonText}" Style="{StaticResource SoftButton}" Command="{Binding ToggleEditingCommand}" HorizontalAlignment="Left" Margin="0,0,10,0" />
                        <Button Content="Save profile" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" HorizontalAlignment="Left" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <StackPanel Grid.Column="1">
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <svgc:SvgViewbox Source="/Resources/settings-2.svg" Width="28" Height="28" HorizontalAlignment="Left" />
                        <TextBlock Text="Profile Lock" Style="{StaticResource SectionTitle}" Margin="0,16,0,8" />
                        <TextBlock Text="Fields stay disabled by default. Unlock the form when edits are needed, then save back to LocalDB and the active session." Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <svgc:SvgViewbox Source="/Resources/library-big.svg" Width="28" Height="28" HorizontalAlignment="Left" />
                        <TextBlock Text="Instrux" Style="{StaticResource SectionTitle}" Margin="0,16,0,8" />
                        <TextBlock Text="A teacher-first productivity hub for attendance, grades, materials, calendar planning, and personal task flow." Style="{StaticResource CaptionText}" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <svgc:SvgViewbox Source="/Resources/trash.svg" Width="28" Height="28" HorizontalAlignment="Left" />
                        <TextBlock Text="Danger Zone" Style="{StaticResource SectionTitle}" Foreground="{StaticResource DangerBrush}" Margin="0,16,0,8" />
                        <TextBlock Text="Permanently delete your account and all associated data. This action cannot be undone." Style="{StaticResource CaptionText}" TextWrapping="Wrap" Margin="0,0,0,16" />
                        <Button Content="Delete account" Style="{StaticResource PrimaryButton}" Background="{StaticResource DangerBrush}" Command="{Binding DeleteAccountCommand}" HorizontalAlignment="Left" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
